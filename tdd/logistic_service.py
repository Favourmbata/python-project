# def get_wage(amount_per_parcel):
#     if amount_per_parcel <= 50:
#         result = (amount_per_parcel * 160) + 5000
#
#     elif 50 >= amount_per_parcel <= 59:
#         result = (amount_per_parcel * 200) + 5000
#
#     elif 60 >= amount_per_parcel <= 69:
#         result = (amount_per_parcel * 250) + 5000
#
#     else:
#         result = (amount_per_parcel * 500) + 5000
#         return result